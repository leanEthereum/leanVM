[alias]
testall = ["test", "--release", "--workspace"]
clippyall = ["clippy", "--release", "--workspace", "--all-targets", "--", "-D", "warnings"]
docall = ["doc", "--release", "--workspace", "--no-deps"]

[build]
rustdocflags = ["-D", "warnings"]

# Not for the RISC-V guests (`guests/`), which inherit this file: no RISC-V target accepts the host's CPU.
[target.'cfg(not(target_arch = "riscv64"))']
rustflags = ["-C", "target-cpu=native"]
